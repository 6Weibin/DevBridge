# 文件管理路径显示和复制调整记录

## 排查步骤

- [x] 检查文件管理树顶部路径和状态小字渲染位置。
- [x] 确认 `已加载 /xxx` 来源于 `fileHint`，对当前界面价值不高。
- [x] 新增路径拆分显示逻辑，长路径只省略前半部分，保留最后一级目录名。
- [x] 增加路径双击复制能力，通过顶部 tips 反馈复制结果。
- [x] 执行前端构建、后端打包和服务重启验证。

## 修复方案

- 移除文件树顶部的 `已加载 /xxx` 小字。
- 路径显示拆成 `prefix` 和 `leaf` 两段：
  - `prefix` 使用省略显示。
  - `leaf` 固定展示，保证最后一级文件夹名称可见。
- 路径区域支持双击复制完整路径。
- 复制成功或失败通过顶部 tips 提示。

## 验证结果

- `DevBridge-Front` 执行 `npx -y yarn@1.22.22 build` 通过。
- `DevBridge-Server` 执行 `mvn package -DskipTests` 通过。
- 后端服务已重启到 `http://127.0.0.1:8080/`。
- 首页返回 `200`，加载新资源 `/assets/index-i98nomQ_.js`。
- `/api/runtime/environment` 返回 `200`。
