# Agent 能力扩展实施任务

## 当前范围

本文件从首个代码任务 M1-01 开始跟踪。实现依据为 `spec.md`、`architecture-assessment.md` 和 `development-task-plan.md`；当前没有独立 `design.md`，因此只实施计划中已经明确的模块骨架，不扩展到后续任务。

## M1-01 Agent Runtime 模块骨架

- [x] 建立与前端、Spring AI、ADB 和 MCP SDK 解耦的 Agent Task 模型。
- [x] 定义 Agent Task Store 端口，支持创建结果保存和按任务 ID 读取。
- [x] 实现 Agent Runtime 创建/读取服务，任务标识由后端生成。
- [x] 提供临时内存 Store 以保证模块可独立运行，持久化替换留给 M1-03。
- [x] 补充任务创建、读取、输入边界和重复 ID 单元测试。
- [x] 执行编译和测试，并对照 `architecture-assessment.md` 完成任务验收。

## 范围外

- M1-02 状态转换器和非法状态转换校验。
- M1-03 本地 Task Store。
- M1-05 Agent Event Sequencer。
- M1-06 REST/SSE API。
- M1-07 敏感确认协调和自动恢复实现。

## M1-02 Agent Task 状态机

- [x] 固化 `spec.md` 定义的合法状态转换表。
- [x] 为任务状态更新增加原因和乐观版本控制。
- [x] 在 Runtime 服务中提供按任务 ID 推进状态的中立入口。
- [x] 覆盖正常完成、等待确认恢复、失败、取消和非法转换测试。
- [x] 执行全量回归并完成架构验收。

## M1-03 本地 Task Store

- [x] 增加 Agent 数据根目录配置并启用本地文件 Store。
- [x] 实现带 Schema 版本和 SHA-256 的任务快照格式。
- [x] 实现同目录临时文件、强制落盘和原子替换。
- [x] 实现任务历史追加记录和可重建分页索引。
- [x] 支持至少 1000 个任务分页，单任务损坏不阻塞其他任务。
- [x] 覆盖重启恢复、版本冲突、临时尾文件和损坏隔离测试。
- [x] 执行全量回归并完成架构验收。

## M1-04 Checkpoint 保存与恢复

- [x] 定义中立 Checkpoint、恢复状态和工具调用快照模型。
- [x] 实现原子 Checkpoint 文件和当前指针。
- [x] 保存当前步骤、已完成步骤、工具状态和待确认信息。
- [x] 实现重启恢复校验，已成功工具调用必须跳过。
- [x] 最新 Checkpoint 损坏时回退到上一个完整版本并隔离损坏文件。
- [x] 覆盖等待确认恢复、工具后恢复、版本校验和损坏回退测试。
- [x] 执行全量回归并完成架构验收。

## M1-05 Agent Event Sequencer

- [x] 定义 Agent Event、事件类型、作用域和关联标识模型。
- [x] 实现单任务严格递增序号和持久事件追加。
- [x] 实现按游标和数量上限补发事件。
- [x] 并发发布不得重复、倒序或跨任务污染。
- [x] 重启后从磁盘高水位继续分配序号。
- [x] 校验工具、模型和确认事件的必要关联标识。
- [x] 执行全量回归并完成架构验收。

## M1-06 Agent Task REST/SSE API

- [x] 实现创建、查询、分页和取消任务 REST API。
- [x] 创建任务时发布 `TASK_CREATED`，取消时发布有序状态事件。
- [x] 实现按游标读取事件的 REST API。
- [x] 实现支持 `Last-Event-ID` 的 SSE 订阅和多订阅者分发。
- [x] UI/SSE 断线不得隐式取消持久任务。
- [x] API 事件序号以字符串输出，避免 JavaScript 精度问题。
- [x] 覆盖创建、查询、取消、游标和 SSE 接口测试。
- [x] 执行全量回归并完成架构验收。

## M1-07 敏感确认迁入 Agent Runtime

- [x] 定义确认绑定、风险和决策状态模型。
- [x] 实现确认记录原子文件持久化和状态条件更新。
- [x] 请求确认时进入 `WAITING_CONFIRMATION` 并保存 Checkpoint。
- [x] 用户批准后后端自动恢复 `RUNNING` 并调用续跑入口。
- [x] 用户拒绝或确认过期后停止依赖执行并输出明确状态。
- [x] 重复批准必须幂等，不得重复调用续跑入口。
- [x] 实现批准和拒绝 REST API。
- [x] 执行全量回归并完成架构验收。

## M1-08 步骤幂等和工具去重

- [x] 在 Checkpoint 工具状态中增加请求摘要和预留状态。
- [x] 相同 `toolCallId` 或幂等键重复请求返回已有记录。
- [x] 相同键但参数摘要不同必须拒绝执行。
- [x] 工具完成结果持久化后，重启仍能直接复用。
- [x] `UNKNOWN` 副作用状态不得被视为可安全重试。
- [x] 覆盖重复请求、参数冲突、完成结果和重启去重测试。
- [x] 执行全量回归并完成架构验收。

## M1-09 跨步骤前置/后置校验

- [x] 定义前置/后置条件、失败动作和结构化校验结果。
- [x] 建立可扩展 Condition Probe 注册边界。
- [x] 实现设备在线和本机路径存在内置 Probe。
- [x] 前置条件失败时按策略重新规划或失败。
- [x] 后置条件失败时不得把步骤标记为成功。
- [x] 覆盖通过、重新规划、失败和未知 Probe 测试。
- [x] 执行全量回归并完成架构验收。

## M1-10 补偿动作框架

- [x] 定义已完成步骤、补偿动作、处理器和结构化结果。
- [x] 可补偿步骤必须按原执行顺序的逆序处理。
- [x] 不可逆步骤只记录剩余影响，不伪造回滚。
- [x] 需要确认的补偿动作不得直接执行。
- [x] 每个补偿结果发布 `STEP_COMPENSATED` 事件。
- [x] 单个补偿失败应记录并继续处理其他可补偿步骤。
- [x] 覆盖逆序、不可逆、确认阻塞和失败继续测试。
- [x] 执行全量回归并完成架构验收。

## M1-11 有界命令输出读取

- [x] 新增共享的字节级有界输出读取器，超限后继续消费进程流但不再占用堆内存。
- [x] 记录 stdout/stderr 的总量、保留量、丢弃量和截断状态。
- [x] `CommandRunner` 使用通用默认上限，避免普通命令输出无界增长。
- [x] ADB 与 Local Shell 非流式执行在读取阶段应用工具计划的输出上限。
- [x] 覆盖单行超长、行数超限和 1 GiB 虚拟输出流测试。
- [x] 执行全量回归并完成架构验收。

## M1-12 有界工具执行线程池

- [x] 新增 Spring 管理的工具调度、命令 IO 和超时调度线程池。
- [x] 工具调度和命令 IO 使用有界队列及明确拒绝策略。
- [x] ADB、Local Shell、通用命令和流式命令统一使用受管 Executor。
- [x] 队列饱和时停止已启动进程并返回可诊断错误，不得静默丢任务。
- [x] 应用退出时关闭线程池并清理等待任务。
- [x] 覆盖并发上限、排队、拒绝和关闭测试。
- [x] 执行全量回归并完成架构验收。

## M1-13 进程树终止

- [x] 新增基于 Java `ProcessHandle` 的跨平台进程树终止器。
- [x] 先终止全部后代进程，再终止父进程，并在宽限期后强制清理。
- [x] 通用命令、流式命令、ADB、Local Shell 和二进制文件命令统一接入。
- [x] 工具取消注册表保存进程树终止句柄，不再只销毁父进程。
- [x] 覆盖 Java 父子进程树的真实终止测试，避免依赖特定 Shell。
- [x] 执行全量回归并完成架构验收。

## M1-14 工具取消传播

- [x] 定义任务级 `CancellationScope`、取消句柄类型和结构化结果。
- [x] 支持模型、工具和进程句柄注册、注销、幂等取消及晚注册立即取消。
- [x] Agent Task 创建时建立取消作用域，用户取消时先进入终态再传播取消。
- [x] 取消回调异常必须隔离并记录，不阻塞其他模型或工具句柄。
- [x] SSE 订阅关闭继续只解除订阅，不隐式取消持久任务。
- [x] 覆盖模型、工具、进程、重复取消、晚注册和回调失败测试。
- [x] 执行全量回归并完成架构验收。

## M1-15 Agent Event 背压

- [x] 新增独立 Spring 管理的 Agent Event 有界执行器。
- [x] 单个 SSE 订阅使用有界实时事件队列和唯一发送任务。
- [x] 历史回放与实时事件保持序号去重，连接重建可按游标补拉。
- [x] 慢客户端队列满时只关闭该订阅，不阻塞事件持久化和工具线程。
- [x] 每批最多发送固定数量事件，避免单订阅长期占用发送线程。
- [x] 提供活动订阅、排队事件、已发送事件和慢订阅关闭指标。
- [x] 覆盖慢客户端、正常排空和持久事件完整性测试。
- [x] 执行全量回归并完成架构验收。

## M1-16 资源锁与任务调度

- [x] 定义设备、本机路径、端口和应用资源键及共享/独占模式。
- [x] 实现多资源稳定排序和原子申请，避免交叉申请死锁。
- [x] 同资源共享读可并发，独占写与其他读写互斥。
- [x] 获取超时返回当前持有任务和资源诊断，不无限等待。
- [x] 租约到期自动释放，任务完成或取消可幂等主动释放。
- [x] 空闲资源状态自动清理，长期运行内存保持有界。
- [x] 覆盖共享、独占等待、超时、租约过期和多资源无死锁测试。
- [x] 执行全量回归并完成架构验收。

## M1-17 本地控制面认证

- [x] 后端启动时生成或读取高熵会话令牌，令牌不写日志和业务文件。
- [x] `/api/**` 校验本机 Host、受信 Origin 和常量时间令牌。
- [x] 同源浏览器首页通过 HttpOnly、SameSite=Strict 会话 Cookie 获取令牌。
- [x] Electron 启动后端时通过环境变量传递令牌，并为浏览器/API 健康检查注入请求头。
- [x] 无令牌、错误令牌、错误 Host 和错误 Origin 返回明确 401/403。
- [x] OPTIONS 预检只校验 Host/Origin，实际请求仍必须认证。
- [x] 覆盖请求头、Cookie、Host、Origin、预检和关闭后令牌失效测试。
- [x] 执行后端、前端构建和 Electron 语法验收。

## M1-18 Prompt 安全分层

- [x] 增加后端不可变安全 Prompt 和版本、摘要。
- [x] 增加版本化产品 Prompt，不再与用户配置共用同一安全层。
- [x] 将旧 `systemPrompt` 配置字段解释为用户风格和输出偏好，保持前端 API 兼容。
- [x] Provider Gateway 按安全、产品、用户偏好的固定顺序组装 system message。
- [x] 用户偏好层明确标记为不可信配置，并破坏伪造的分层边界标记。
- [x] 覆盖忽略安全规则、自动执行高风险命令和任意数据外发测试。
- [x] 执行全量回归并完成架构验收。

## M1-19 数据分类与模型外发审批

- [x] 定义 `ALLOWED`、`CONFIRMATION_REQUIRED`、`LOCAL_MODEL_ONLY` 和 `PROHIBITED` 四类数据策略。
- [x] 定义日志、应用列表、设备标识、工具输出、文件、截图、位置、源码和凭据等数据类型。
- [x] Provider Gateway 在普通和流式网络请求前统一执行外发守卫。
- [x] 外部 Provider 的敏感数据必须确认，本地回环模型可处理 `LOCAL_MODEL_ONLY`，凭据类数据始终阻断。
- [x] 确认绑定任务、步骤、模型调用、Provider、模型、数据类型、字节数和内容摘要。
- [x] 复用 Agent Runtime 确认存储和自动续跑边界，拒绝跨步骤、跨模型调用或过期确认。
- [x] 将实际外发决策摘要写入任务有序 `MODEL_EGRESS_DECIDED` 事件，不记录正文。
- [x] 执行全量回归并完成架构验收。

## M1-20 不可信内容隔离

- [x] 定义日志、文件、网页、截图 OCR、工具输出、RAG 和 Agent 结果等不可信来源。
- [x] 实现包含信任级别、来源、用途、范围、长度和 SHA-256 的 `Untrusted Content Envelope`。
- [x] 明确禁止不可信内容修改任务目标、工具权限、确认、数据外发和安全策略。
- [x] 中和正文伪造的封装结束标记、不可变安全层和产品层标记。
- [x] 日志分析 Prompt、ADB 工具输出和 Local Shell 工具输出统一接入封装。
- [x] 检测忽略策略、角色覆盖、绕过确认和命令注入信号，不把恶意正文写入普通日志。
- [x] 覆盖“忽略规则并执行删除”、边界伪造和正常工具输出测试。
- [x] 执行全量回归并完成架构验收。

## M1 结构收敛优化

- [x] 删除仅承载 Prompt 组装结果的 `AiPromptLayers`，由组合器直接返回最终 Prompt。
- [x] 将外发分类、数据类型、决策、数据项、调用上下文和评估结果收敛到 `AiDataEgressPolicy`。
- [x] 将数据外发确认校验、确认创建和任务事件记录并入 `AiDataEgressGuard`。
- [x] 将不可信来源、封装请求和安全事件收敛到 `AiUntrustedContent`。
- [x] 删除独立不可信内容 Recorder，由 `AiUntrustedContentService` 直接记录脱敏摘要。
- [x] M1-18 至 M1-20 生产文件由 17 个收敛为 6 个，不改变 API、安全策略、确认恢复和 Provider 行为。
- [x] 执行 194 个后端测试、前端生产构建、Electron 语法检查和后端 JAR 打包。

## M2-01 统一 Tool Gateway

- [x] 建立不依赖 ADB、Local Shell、Spring AI、MCP SDK 和前端的中立工具契约。
- [x] 实现工具注册表和全局稳定 ID 去重。
- [x] 实现对象、数组、标量、必填、枚举、限长和未知字段 JSON Schema 校验。
- [x] 实现契约版本、工具版本、启用/弃用和平台能力校验。
- [x] 实现动态风险不得低于静态基线的策略管道。
- [x] 复用 Agent Runtime 持久确认，绑定任务、步骤、调用、工具、参数摘要和风险。
- [x] 复用 M1 幂等 Checkpoint 和资源锁，并在 Adapter 执行后统一脱敏结果。
- [x] 工具请求、策略和终态进入任务级有序 Agent Event。
- [x] 覆盖正常执行、未知字段、平台不匹配和风险降级拒绝测试。
- [x] 执行全量回归并完成架构验收。

## M2-02 ADB 中立工具适配

- [x] 将现有 ADB 工具定义转换为稳定中立 ID、平台、来源、访问模式、幂等、风险和执行元数据。
- [x] 复用 ADB Planner 和风险分类器生成 Gateway 动态风险决策。
- [x] 设备工具按读写属性申请共享或独占设备锁。
- [x] 增加包内 Gateway 批准执行入口，执行前仍重新规划、校验设备和计算风险。
- [x] 将 ADB 结果转换为中立结果，不再向上层暴露旧确认令牌和 MCP 标题。
- [x] 保留旧 ADB REST/Spring AI 兼容链路，不影响现有功能回退。
- [x] 覆盖定义元数据、删除命令动态风险和中立结果转换测试。
- [x] 执行全量回归并完成架构验收。

## M2-03 Local Shell 中立工具适配

- [x] 将现有 Local Shell 工具定义转换为稳定中立 ID、独立来源、平台、访问模式和幂等元数据。
- [x] 复用 Local Shell Planner 和用户授权配置计算 LOW、MEDIUM、HIGH 风险决策。
- [x] 将既有 CRITICAL 风险映射为中立 HIGH/BLOCK，禁止模型绕过本地阻断策略。
- [x] 按本机路径读写属性申请共享或独占资源锁。
- [x] 增加包内 Gateway 批准执行入口，执行前重新规划并校验工作目录和动态风险。
- [x] 将 Local Shell 结果转换为中立结果，不再向上层暴露旧确认令牌和 MCP 标题。
- [x] 保留旧 Local Shell REST/Spring AI 兼容链路，不影响现有功能回退。
- [x] 覆盖独立来源、用户授权风险映射、阻断策略和中立结果转换测试。
- [x] 执行 204 个后端测试并完成架构验收。

## M2-04 工具 Schema 版本和能力元数据

- [x] 增加严格三段语义化版本解析和兼容性判断。
- [x] 未知主版本、未来版本和非法版本在策略与 Adapter 执行前明确拒绝。
- [x] 旧版同主版本调用必须由所属 Adapter 显式迁移，不允许静默兼容。
- [x] 迁移后重新生成工具引用和参数摘要，使确认与幂等绑定实际执行参数。
- [x] 增加平台、能力标签和访问模式组合查询，供 Router 与标准 MCP Adapter 复用。
- [x] 查询结果包含稳定 ID、显示名、Schema、来源、平台、读写、幂等、风险、执行和弃用元数据。
- [x] 禁用和弃用工具默认不进入普通发现结果。
- [x] 覆盖旧版迁移、未知主版本拒绝和能力查询测试。
- [x] 执行 207 个后端测试并完成架构验收。

## M2-05 标准 MCP Server Adapter

- [x] 引入 Spring AI 1.0.9 官方 WebMVC MCP Server Starter，不在领域核心中直接依赖 MCP SDK。
- [x] 将中立 Tool Gateway 映射为标准 `tools/list` 和 `tools/call`。
- [x] 工具名称使用全局稳定中立 ID，输入 Schema 复用统一定义。
- [x] 使用 `_devbridge` 协议包装传递平台、设备、工作区、任务、确认和幂等上下文，调用前从业务参数移除。
- [x] 标准 MCP 端点位于 `/api/ai/mcp/standard/**`，复用本地 Host、Origin 和进程令牌认证。
- [x] 未授权 SSE 请求返回 401，授权客户端可完成 `initialize`、`tools/list` 和 `tools/call`。
- [x] 实际协议调用 `android.adb.version` 成功，执行链路仍经过 Tool Gateway 和本地风险策略。
- [x] 修复 JAR 启动时 `AgentEventSubscriptionService` 生产构造器未明确注入的问题。
- [x] 修正 Local Shell 参数分型工具静态风险基线，用户 LOW 授权可按配置执行。
- [x] 执行 210 个后端测试、JAR 打包和真实 MCP SSE 协议验收。

## M2-06 Capability Registry 和结构化 Router

- [x] 复用统一 Tool Registry 作为平台和工具能力注册表，不复制工具目录。
- [x] 定义领域、执行模式、模型能力、Agent 能力、目标平台和结构化路由结果。
- [x] 提供严格 Router JSON Schema，拒绝未知字段和自由指令字段。
- [x] Router 不解析自然语言关键词，只消费经过 Schema 校验的结构化计划。
- [x] 按平台、连接状态、能力标签、访问模式和最大风险选择最小工具集合。
- [x] 模型不支持 Tool Calling 时不得进入工具执行模式。
- [x] 候选 Agent 必须同时覆盖领域、目标平台和所需能力。
- [x] 同时涉及 Android 与 macOS 的请求可输出独立工具选择，不暴露无关应用写工具。
- [x] 旧单轮聊天关键词路由仅作为兼容链路保留，新 Agent Runtime 使用结构化 Router。
- [x] 执行 213 个后端测试并完成架构验收。

## M2-12 Tool Artifact Store

- [x] 增加独立 Artifact 根目录配置和 UUID 隔离目录。
- [x] 使用 4 MiB 固定原始分段和 64 KiB 有界缓冲流写入大输出。
- [x] 支持 `none` 和逐段 `gzip` 压缩，SHA-256 按原始内容计算。
- [x] 元数据与分段在临时目录完成后原子发布，失败时隔离清理。
- [x] Artifact 引用包含媒体类型、原始大小、摘要、压缩、敏感级别和保留时间。
- [x] 支持跨分段范围读取，单次最多 1 MiB，模型和前端不获取真实文件路径。
- [x] 提供受本地控制面认证保护的元数据、范围内容和读取限制 API。
- [x] 100 MiB 虚拟输入测试不创建完整内容数组，验证 25 个分段和跨段读取。
- [x] 执行 215 个后端测试并完成架构验收。

## M2-07 Device 领域工具

- [x] 增加平台中立的设备列表、详情、健康、截图和连接诊断工具。
- [x] 工具来源统一为 `device-service`，显示名和结果不暴露 ADB/MCP 协议术语。
- [x] Android 详情走 `AndroidDeviceService`，iOS 详情走 `IosDeviceService`。
- [x] 健康结果区分 `HEALTHY`、`ATTENTION` 和指标不足的 `UNKNOWN`，不对缺失指标误判。
- [x] 截图定义为 MEDIUM 风险，必须确认后执行。
- [x] 截图文件立即转为受控 Artifact，只返回 ID、媒体类型和引用，不返回本机路径。
- [x] 设备列表与诊断使用宿主共享锁，单设备读取使用设备共享锁。
- [x] 设备序列号参数必须与执行上下文设备一致。
- [x] 覆盖领域定义、iOS 路由和截图 Artifact 测试。
- [x] 执行 218 个后端测试并完成架构验收。

## M2-08 Log 领域工具

- [x] 在既有 `LogStreamService` 增加非 HTTP 的 Agent 采集、状态、文件快照和会话导出入口。
- [x] Agent 与页面 SSE 复用同一底层进程、滚动文件、单设备会话和清理逻辑。
- [x] 提供启动、读取、停止、状态和导出 5 个结构化日志工具。
- [x] 读取工具支持级别、文本和最大行数过滤，最多读取 64 KiB/2000 行。
- [x] 日志尾部按文件范围读取，不把完整滚动文件载入内存。
- [x] 导出工具先停止底层进程，再将完整 zip 写入 Artifact 并清理临时文件。
- [x] 启动、停止、导出使用设备独占锁；读取和状态使用共享锁。
- [x] 启动和导出声明幂等键，避免恢复或重试重复创建进程和 Artifact。
- [x] 覆盖完整生命周期、过滤读取、Artifact 导出和底层进程停止测试。
- [x] 执行 222 个后端测试并完成架构验收。

## M2-09 App 领域工具

- [x] 提供应用列表、详情、安装、卸载、启动、停止和权限读取 7 个 Android 领域工具。
- [x] 查询工具 LOW 直接执行；安装、卸载、启动和停止 MEDIUM 等待用户确认。
- [x] 安装只接受 APK Artifact，不接受模型提供的本机路径。
- [x] Artifact 以流式方式物化为受控临时 APK，安装完成或失败后立即删除。
- [x] Android 服务严格校验包名、APK 普通文件、扩展名和大小上限。
- [x] 安装后验证期望包存在，卸载后验证包消失，启动和停止后检查进程状态。
- [x] 权限工具只返回申请和已授权权限，不外发应用数据目录等无关字段。
- [x] 查询使用设备共享锁，修改和控制使用设备独占锁并声明幂等/验证能力。
- [x] 覆盖风险、Artifact 安装、权限最小输出和服务层后置验证测试。
- [x] 执行 227 个后端测试并完成架构验收。

## M2-10 File 领域工具

- [x] 提供目录、详情、搜索、预览、拉取、上传、删除、重命名和复制 9 个文件领域工具。
- [x] 所有远端路径继续经过 `AndroidPathGuard` 归一化，拒绝相对路径、`..` 和控制字符。
- [x] 搜索条件拒绝路径分隔符、通配符和控制字符，深度与结果数量有界。
- [x] 文本预览最多读取 64 KiB，不加载完整远端文件。
- [x] 拉取文件写入压缩 Artifact，上传文件只从 Artifact 流式物化。
- [x] 上传默认拒绝覆盖并读取目标详情做后置验证。
- [x] 删除只允许单文件，重命名只允许同目录单级名称，复制自动生成不覆盖名称。
- [x] 文件读取使用共享设备锁，上传、删除、重命名和复制使用独占锁并等待确认。
- [x] 覆盖工具定义、Artifact 拉取/上传、搜索注入拒绝和上传后置验证测试。
- [x] 执行 232 个后端测试并完成架构验收。

## M2-11 iOS 与 HarmonyOS 平台适配器

- [x] 增加独立 `HarmonyDeviceService`，通过 HDC 白名单参数读取连接和设备详情。
- [x] HDC 命令使用参数数组和显式目标序列号，不拼接 Shell 字符串。
- [x] Device 领域详情和健康工具增加 HarmonyOS 平台支持。
- [x] 增加 iOS/libimobiledevice 与 HarmonyOS/HDC 平台 Adapter。
- [x] iOS 平台 Adapter 提供详情后端和日志能力检查。
- [x] HarmonyOS 平台 Adapter 提供详情后端和连接诊断。
- [x] Router 对同一能力优先领域服务，只在领域服务缺失时选择平台 Adapter。
- [x] iOS/Harmony 平台能力查询不返回 `android.adb.*` 工具。
- [x] 未实现的 iOS/Harmony App/File 能力返回结构化 `CAPABILITY_UNAVAILABLE`，不回退 ADB。
- [x] 覆盖平台来源、Harmony 详情、HDC 参数化命令、平台隔离和领域优先测试。
- [x] 执行 237 个后端测试并完成架构验收。

## M2-13 统一 Storage Manager

- [x] 增加 20 GiB 默认统一配额配置，覆盖 Agent、Artifact、日志、下载、配置和 RAG 分类。
- [x] 配置根目录只统计直接配置文件，避免与其子目录 Agent/Artifact/RAG 重复计数。
- [x] 80% 返回 WARNING，仅预警不删除数据。
- [x] 95% 返回 CRITICAL，显式清理只处理过期 Artifact、下载临时文件和历史日志。
- [x] 100% 返回 FULL，拒绝新写入，不通过截断业务结果规避配额。
- [x] 增加并发流式写入许可，多个 Artifact 写入共同受总配额约束。
- [x] Artifact Store 按输入分块持续预留配额，失败时回滚内存预留和临时目录。
- [x] Agent Task 分类永不自动清理，调用方保护路径也不会被删除。
- [x] 提供分类占用查询和显式受控清理 API。
- [x] 覆盖阈值、配额拒绝、过期清理和活跃任务保护测试。
- [x] 执行 240 个后端测试并完成架构验收。

## M2-14 结构化持久审计

- [x] 增加按 UTC 日期追加的 JSONL 工具审计文件，每行带 Schema 版本和 SHA-256 完整性校验。
- [x] 审计只保存任务、步骤、工具调用、工具、参数摘要、风险决策、状态、耗时、脱敏摘要和错误码。
- [x] 不保存完整工具参数、完整输出、密钥、令牌和异常堆栈，摘要统一经过敏感数据脱敏和长度限制。
- [x] Tool Gateway 在策略与执行前写入请求审计，审计不可写时关闭执行入口。
- [x] 工具完成或拒绝后写入终态审计；终态写入失败不触发可能有副作用的工具重放。
- [x] 提供按任务、工具、风险和时间筛选的查询接口，以及 90 天整日文件保留清理接口。
- [x] 单行 JSON 或校验和损坏时隔离该行，不阻断同文件其他有效记录查询。
- [x] 审计目录纳入统一 Storage Manager 的 AUDIT 分类统计。
- [x] 覆盖重启查询、组合筛选、敏感信息不落盘、损坏行隔离和过期清理测试。
- [x] 执行 244 个后端测试并完成架构验收。

## M2-15 操作系统密钥环

- [x] 增加单一聚合式系统密钥环组件，不按操作系统拆分多层 Adapter 文件。
- [x] macOS 使用 Keychain `security`，密钥通过标准输入提供，不进入命令参数和日志。
- [x] Windows 使用 Credential Manager 的 PasswordVault，PowerShell 以标准输入接收密钥。
- [x] Linux 使用 Secret Service `secret-tool`，密钥通过标准输入写入。
- [x] AES-GCM 密文格式保持不变，旧 `ai-config.key` 可继续解密并在首次使用时迁移到系统密钥环。
- [x] 已有配置但系统密钥缺失或不可用时明确失败，不静默生成新密钥导致配置永久不可恢复。
- [x] 系统密钥环不可用时原子写入回退文件，POSIX 权限为目录 700、文件 600，Windows ACL 仅允许当前所有者。
- [x] Spring 启动阶段创建并校验配置目录和已有回退密钥权限，不输出密钥、命令输入或系统错误正文。
- [x] 覆盖三平台命令边界、跨实例解密、旧文件迁移、安全回退权限和禁止静默换密钥测试。
- [x] 执行 249 个后端测试、后端打包、前端构建、Electron 语法和隔离 JAR 启动验收。

## M2.5-01 当前 Chat API 纳入 Agent Task 生命周期

- [x] 普通和流式 Chat API 在调用 Provider 前创建持久 Agent Task。
- [x] 新任务统一经过 CREATED、PLANNING 和 RUNNING，正常完成和 Provider 异常进入明确终态。
- [x] Provider ToolContext 和数据外发上下文携带 taskId、conversationId、turnId、stepId 和 modelCallId。
- [x] 首个流式事件返回 taskId，现有前端聊天、Markdown 和工具事件协议保持兼容。
- [x] 敏感确认暂停后，前端兼容续跑请求回传 taskId，后端恢复同一任务而不是创建第二个任务。
- [x] 恢复时校验 conversationId，阻止跨历史会话接管等待确认任务。
- [x] 没有新增生产类；只修改现有 Chat、Task 应用服务和前端 API 边界。
- [x] 新增 4 个任务生命周期和 Chat 主链路测试，全量 253 个后端测试通过，前端生产构建通过。

## M2.5-02 当前模型工具调用迁入 Tool Gateway

- [x] `AiToolRegistry` 改为从统一 Tool Gateway 发现工具，不再组合旧 ADB/Local Shell Spring Callback。
- [x] 当前设备平台和本机平台共同限制工具暴露范围，普通设备问题不会额外暴露宿主机工具。
- [x] Spring AI ToolCallback 将 JSON 参数、Agent 身份、设备、工作目录和确认身份转换为中立 CallRequest。
- [x] 当前 Chat 中的 ADB、Local Shell 和领域工具统一经过 Schema、平台、风险、确认、幂等、资源锁、脱敏和审计。
- [x] 中立 CallResult 映射为现有工具卡片结果，前端过程时间线无需重写。
- [x] 工具结果继续进入不可信内容封装，模型不能把工具输出中的指令当成授权。
- [x] Agent 确认兼容令牌绑定 taskId、confirmationId、stepId 和 toolCallId。
- [x] 批准后的第二次模型调用复用原步骤和工具调用标识，使确认摘要和幂等 Checkpoint 命中原请求。
- [x] 前端批准/拒绝 Agent Confirmation 使用正式任务确认 API；旧确认接口仅为旧 REST 兼容保留。
- [x] 没有新增生产文件；新增 2 个 Gateway Chat 适配测试，全量 257 个后端测试、前端构建和隔离 JAR 启动通过。

## M2.5-03 Agent Event 接管聊天过程事件

- [x] 普通和流式 Chat 发布模型调用开始、完成、失败以及输出开始、完成事件。
- [x] 高频模型正文继续通过兼容 SSE 分片直传，Agent Event 只持久化控制事件、输出字符数和结束原因，避免重复正文占用磁盘和内存。
- [x] 敏感操作进入等待确认前闭合当前模型和输出生命周期，结束原因为 `WAITING_CONFIRMATION`。
- [x] SSE 超时、Provider 回调和确认暂停使用同一流终态标记，正常完成后不会被异步回调重复写成失败。
- [x] Provider 调用异常与 Agent 控制面持久化异常分开处理，事件写入失败不会覆盖 Provider 原始错误或伪造模型失败事件。
- [x] 页面完成或连接异常只清理兼容工具事件订阅，不取消仍在运行的持久 Agent Task；真正 Provider 异常仍由模型监听器收尾。
- [x] 流式并发状态收敛到 `AiConversationService` 内部单一状态对象，没有新增生产文件或新的框架层。
- [x] 新增 3 个事件生命周期和故障注入测试，全量 260 个后端测试通过，后端打包、前端构建、Electron 检查和隔离 JAR 启动通过。

架构评估自检：

- [x] 符合“Agent 状态和顺序进入后端控制平面”，前端没有新增任务状态机或 Prompt 编排。
- [x] 符合“UI 连接、任务和工具生命周期解耦”，页面断开不再直接取消持久任务。
- [x] 符合“Agent Event 有界化”，高频正文不逐 Token 持久化，控制事件仍具备任务、步骤和模型调用关联标识。
- [x] 符合“保留模块化单体并避免过度架构”，只修改现有 Chat 服务和测试，没有新增生产文件、事件总线或中间层。
- [x] 符合“Provider 错误与控制面错误可观测且不混淆”，原始 Provider 异常优先，持久化错误使用独立诊断码。

## M2.5-04 后端确认自动续跑

- [x] Chat 新轮次把原始问题、设备摘要和最近有界文本历史保存到现有 Agent Checkpoint。
- [x] 恢复快照限制为 12 条、总计 8000 字符，并在落盘前脱敏常见凭证、邮箱和手机号。
- [x] 确认批准 API 改为 SSE，直接调用后端 `AgentRuntimeContinuationService`，不再要求前端发起第二次 Chat 请求。
- [x] 后端从 Checkpoint 重建同一 `taskId`、设备和历史，生成绑定原确认、步骤和工具调用的内部令牌。
- [x] `AiToolRegistry` 复用授权前校验任务、确认状态、工具、参数摘要、步骤和工具调用标识。
- [x] 修改参数或切换工具不能消费旧确认；已完成工具继续由 Checkpoint 幂等服务阻止重放。
- [x] 前端批准后直接消费后端续跑流，删除前端 `taskId/confirmationToken` 续跑参数和内部 Prompt。

架构评估自检：

- [x] 符合“确认续跑进入后端控制平面”，前端只提交批准决策并展示事件。
- [x] 符合“Checkpoint 可恢复”，服务重启后仍可恢复原问题、设备和最近历史。
- [x] 符合“安全策略不能由模型决定”，授权绑定由 Tool Gateway 和确认 Store 强制校验。
- [x] 符合“上下文和磁盘有界”，没有保存完整工具输出、Provider 密钥或逐 Token 正文。

## M2.5-05 删除双轨并收敛碎片模型

- [x] 删除未被生产代码引用的 `SpringAiAdbToolCallbackAdapter`。
- [x] 删除未被生产代码引用的 `SpringAiLocalShellToolCallbackAdapter`。
- [x] 删除只有单一实现、已经不再承担解耦价值的 `AgentTaskContinuation`。
- [x] 删除前端 `confirmationContinuationPrompt`、工具时间线 Prompt 和旧确认自动续跑分支。
- [x] 将设备、历史和续跑上下文作为嵌套契约聚合到 `AgentRecoveryState`，未横向新增模型文件。
- [x] 保留 ADB/Local Shell REST 与 MCP 兼容服务，避免误删仍被当前业务使用的执行、确认和审计能力。

架构评估自检：

- [x] 符合“最小化改动和避免过度封装”，本阶段净删除 3 个生产文件，没有新增生产文件。
- [x] 符合“统一 Tool Gateway”，当前模型工具调用不存在旧 Callback 旁路。
- [x] 符合“模块化单体”，没有新增微服务、消息中间件或第二套工作流引擎。

## M2.5-06 业务级端到端验收

- [x] 普通同步、流式和多轮上下文测试通过。
- [x] 日志分析、日志领域工具、设备领域工具和应用领域工具测试通过。
- [x] ADB Tool Gateway、风险、确认和执行测试通过。
- [x] Local Shell Tool Gateway、风险、确认和执行测试通过。
- [x] 确认批准、后端续跑、授权绑定、参数篡改拒绝和工具幂等测试通过。
- [x] Checkpoint 当前格式、旧格式、跨实例恢复、损坏回退和已完成工具跳过测试通过。
- [x] 全量 265 个后端测试通过，0 失败、0 错误、0 跳过。
- [x] 后端打包、前端 Vite 构建、Electron 主进程语法检查通过。
- [x] 隔离 JAR 启动成功，HTTP 返回 200，MCP 注册 54 个工具；验收服务已停止。

架构评估自检：

- [x] 当前 Chat 主链路已使用唯一 Agent Task、Agent Event、Tool Gateway、Confirmation 和 Checkpoint 控制面。
- [x] 手机 ADB 与电脑 Local Shell 继续共用统一风险、确认、幂等、锁、脱敏和审计策略。
- [x] UI 不再决定任务恢复或构造内部 Agent Prompt，符合目标架构职责边界。
- [x] 本阶段没有提前引入多 Agent、动态工作流、RAG 或新的 Provider 抽象。

二次验收返修：

- [x] 确认批准后从加密 Checkpoint 恢复原始 `CallRequest`，由后端直接执行 Tool Gateway，再把真实结果交给模型继续总结。
- [x] 确认状态增加已消费语义，同一进程使用活动键阻断并发续跑；重启后依靠原 `toolCallId` 和持久幂等结果恢复。
- [x] 工具请求、工具结果和最终回复使用 AES-GCM 受保护存储；任务目标和设备摘要落盘前脱敏。
- [x] Agent Task 在进入 `COMPLETED` 的同一任务快照中保存最终回复，并提供结果查询接口供 SSE 断线恢复。
- [x] 前端显式取消调用 Agent Task 取消接口；Provider、ADB 和 Local Shell 可取消句柄接入统一取消作用域。
- [x] 工具事件订阅从 `conversationId` 收敛为 `taskId + turnId`，避免同会话并发流覆盖。
- [x] 同步 Chat 禁用需要确认的工具回调，消除同步接口的确认状态缺口。
- [x] 现有确认续跑测试改为确定性执行闭环测试；未新增大批细碎测试，全量 264 个后端测试通过。
- [x] 后端打包、前端 Vite 构建、Electron 语法、隔离 JAR 启动、HTTP 200 和 54 个 MCP 工具注册验收通过。

三次验收稳定性返修：

- [x] 应用启动后扫描确认续跑任务，并从持久 Checkpoint 自动恢复原任务。
- [x] 重启恢复复用原 `toolCallId`、确认绑定和工具幂等结果，不重复执行已完成敏感操作。
- [x] 重复确认遇到终态任务时直接重放既有结果，不再因 Checkpoint 版本差异失败。
- [x] 工具调用前文本不再进入持久化最终回复，断线恢复结果与前端最终回答保持一致。
- [x] 前端取消失败时不再中止本地流或显示取消成功，错误详情进入对话区域。
- [x] 仅新增 3 个业务闭环测试，全量 267 个后端测试通过；前端构建和隔离 JAR 启动验收通过。

架构评估自检：

- [x] 恢复调度仍位于后端 Agent Runtime，前端没有新增状态机或恢复 Prompt。
- [x] 重启后工具执行继续经过统一 Tool Gateway、风险、确认、幂等、锁、脱敏和审计链路。
- [x] 后台恢复不持有长 SSE 正文，避免增加 Electron 或服务端内存风险。
- [x] 没有新增生产文件、工作流引擎、事件总线或重复抽象，改动限定在现有职责边界内。

## M3-01 历史聊天本地文件存储

- [x] 后端本地文件成为历史聊天唯一真相源，前端不再向 `localStorage` 写入会话。
- [x] 会话正文按文件加密压缩并原子替换，索引支持至少 1000 个会话分页。
- [x] 详情接口只返回有界消息尾部，前端不加载完整 1M Token 历史。
- [x] 前端消息尾部按稳定消息 ID 合并，未加载的旧消息不会被覆盖或丢失。
- [x] 旧浏览器历史迁移成功后才删除；迁移失败保留原数据供重试。
- [x] 新建、续聊、重命名、删除和点击不置顶行为保持不变。
- [x] 会话写入纳入统一 Storage Manager 配额，不引入数据库或新框架。
- [x] 以 3 个业务测试、270 个全量后端测试、前端构建和实际 JAR 接口完成架构评估自检。

## M3-02 Working Memory 和上下文预算

- [x] 后端从 Conversation Store 组装 Working Memory，前端不再回传历史正文。
- [x] 上下文预算由模型窗口、输出 Token、系统/工具预留和当前问题共同计算。
- [x] 最近消息优先，普通用户/AI 文本之外的消息不进入模型上下文。
- [x] 单条长消息和总历史超限时明确截断，完整历史文件不进入模型请求。
- [x] 同一 Working Memory 复用于 Provider Prompt 和确认恢复 Checkpoint。
- [x] 模型调用事件记录预算摘要，不记录历史正文。
- [x] 不提前实现摘要、模型能力注册、RAG 或新的持久化层。
- [x] 以 4 个 Context Builder 测试、274 个全量后端测试、前端构建和实际 JAR 启动完成架构评估自检。

## M3-03 对话与任务摘要

- [x] 摘要分为用户约束、历史对话和任务/工具结果三层。
- [x] 摘要包含 Schema、版本、来源消息数量、首尾消息 ID 和来源校验值。
- [x] 来源前缀未变化时复用摘要，累计批次或来源变化时确定性重建。
- [x] 摘要保存在现有加密会话文件中，不增加数据库和第二套文件索引。
- [x] Context Builder 在预算内组合摘要和最近消息，摘要不会挤占全部最近上下文。
- [x] 确认恢复继续使用原摘要，模型事件不记录摘要正文。
- [x] 不引入额外模型调用、后台任务框架、RAG 或多 Agent。
- [x] 以少量业务测试、277 个全量后端测试、前端构建和隔离 JAR 启动完成架构评估自检。

## M3-04 Device 和 Incident Memory

- [x] 设备快照和故障案例按本地小文件保存，支持按设备、系统版本和故障特征查询及删除。
- [x] 文件采用 GZIP + AES-GCM、原子替换、Schema/类型校验和损坏隔离。
- [x] Memory 纳入统一 Storage Manager 配额，删除失败不会先移除内存索引。

## M3-05 Model Capability Registry

- [x] 运行时配置包含 Chat、Tool Calling、Streaming、多模态、JSON 和模型限制快照。
- [x] embedding、rerank、语音、图像等非聊天模型不会进入对话；能力不兼容模型不会作为降级候选。

## M3-06 模型请求预算

- [x] 上下文和输出预算按模型能力与部署上限取严格值，删除固定 `6000` 输出上限。
- [x] 工具调用在进入 Tool Gateway 前执行有界计数，超限不产生工具副作用。
- [x] 模型事件记录上下文、输出、工具、调用、重试和时间预算，以及实际输出估算、工具次数和重试次数。

## M3-07 结构化输出校验

- [x] Schema 校验支持字符串、数组和数值边界。
- [x] Router 只接受纯 JSON 或 Markdown JSON，最多修复两次；持续非法输出不会进入工具路由。

## M3-08 Provider 分阶段重试与降级

- [x] 仅超时、连接失败、429 和服务端错误允许重试，鉴权、参数和路径错误直接失败。
- [x] 同模型最多重试一次，再选择一个能力兼容且已配置的 Provider。
- [x] 已输出正文或已触发工具调用后禁止重试；流式订阅替换已处理同步错误竞态。

## M3-09 本地 RAG

- [x] 支持文档导入/更新、确定性切分、词法检索、引用、删除和重建。
- [x] 历史 Incident 可作为本地知识来源；RAG 证据在上下文预算内脱敏并按不可信内容进入 Prompt。
- [x] RAG 文件加密压缩并受统一磁盘配额管理；删除失败不会产生重启后数据复活。

## M3-10 设备健康检查工作流

- [x] 固定调用设备详情和健康指标工具，指标缺失时返回 UNKNOWN，不生成虚假评分。
- [x] 报告包含设备证据、健康指标、异常和建议；结果保存设备快照，异常保存 Incident。

## M3-11 实时日志诊断工作流

- [x] 采集窗口限制为 1 至 60 秒，读取最多 1500 行有界日志。
- [x] 识别 Crash、ANR、OOM、权限和网络特征，关联本地知识并保存 Incident。
- [x] 成功、异常和取消路径均执行日志停止，重复停止保持幂等。

## M3-12 跨端构建安装诊断工作流

- [x] 本机构建使用受控 ARGV，APK 必须位于允许工作目录并进入 Artifact Store。
- [x] 安装、启动和日志验证继续经过统一 Tool Gateway，子步骤使用稳定标识和幂等键。
- [x] 父流程进行一次高风险确认；子步骤只在父确认绑定完全匹配且风险覆盖时继承授权，BLOCK 不可绕过。
- [x] 部分失败报告保留已完成步骤、失败原因和已生成 APK Artifact。

## M3 总体验收

- [x] 对照 `architecture-assessment.md`，M3 未引入数据库、向量数据库、消息中间件、通用工作流引擎、新 Provider 抽象或前端状态机。
- [x] 主要生产新增类控制为 5 个，其余修改复用既有 Context Builder、Router、Provider Gateway、Storage Manager、Tool Gateway 和策略流水线。
- [x] 286 个后端测试通过，0 失败、0 错误、0 跳过；前端 Vite 构建和后端打包通过。
- [x] 隔离 JAR 在 0.951 秒内启动并返回 HTTP 200，MCP 注册 57 个工具；Memory/RAG API 和磁盘密文完成实际验收，隔离服务已停止。

## M4-01 Agent Registry

- [x] 新增后端权威 `AiAgentRegistry`，注册设备健康、日志诊断和构建安装三个已可执行固定工作流 Agent。
- [x] Agent 定义包含领域、平台、工具能力、访问模式、最高风险、模型约束和数据权限。
- [x] Registry 可按 Router 已选择工具筛选候选 Agent，并提供单 Agent 工具授权和数据权限查询。
- [x] `ToolCapabilityRouter` 不再信任请求中由模型提交的 Agent 能力；伪造 Agent 不能获得工具权限。
- [x] 普通对话和直接工具模式保持不变，没有提前实现 Supervisor、专业 Agent Prompt 或多 Agent 调度。
- [x] 只新增 1 个生产类、修改 1 个现有 Router；2 条新增业务测试后，全量 288 个后端测试通过。
- [x] 后端打包和隔离 JAR 启动通过，HTTP 返回 200，MCP 工具数量保持 57；隔离服务已停止。

## M4-02 Supervisor Agent

- [x] 新增受限 Supervisor 服务，结构化计划最多 8 步，步骤 ID 必须唯一且按输入顺序执行。
- [x] 计划只允许填写后端注册 `agentId`，不能直接指定 ADB、Local Shell 或其他底层工具。
- [x] Worker 的唯一高层工具由 Agent Registry 权限反向解析，所有调用继续经过统一 Tool Gateway。
- [x] Supervisor 动态风险取全部 Worker 最高值，高风险计划在入口确认；嵌套 Worker 传递最外层确认绑定。
- [x] 支持失败立即停止或继续后续步骤，最终明确返回 `SUCCEEDED`、`PARTIAL` 或 `FAILED`，并保留 Worker 结果和错误详情。
- [x] 计划和步骤复用 `PLAN_CREATED`、`STEP_STARTED`、`STEP_COMPLETED/FAILED` 有序事件，委派过程可观测。
- [x] 产品 Prompt 在多个固定工作流场景下优先使用 Supervisor，不改变普通对话和单工作流路径。
- [x] 仅新增 1 个生产类并复用现有工作流适配器；2 条新增闭环测试后，全量 290 个后端测试通过。
- [x] 后端打包和隔离 JAR 启动通过，HTTP 返回 200，MCP 工具增加为 58；隔离服务已停止。

## M4-03 Device/Log/App/Local 专业 Agent

- [x] Agent Registry 区分 Supervisor 入口能力和 Worker 内部工具能力，并新增四个专业 Agent 指令与数据权限。
- [x] Device Agent 只允许设备列表、详情、健康、截图和连接诊断 Operation。
- [x] Log Agent 只允许日志启动、读取、停止、状态和导出 Operation。
- [x] App Agent 只允许应用列表、详情、权限、安装、卸载、启动和停止 Operation。
- [x] Local Agent 只允许受控目录、文本、进程和本机命令 Operation；`EXEC` 使用高风险入口确认。
- [x] 模型不能指定底层工具 ID；Operation 经固定白名单映射后再次通过 Registry Worker 权限校验。
- [x] 中高风险操作显示脱敏参数摘要并确认，底层工具继承最外层确认且 BLOCK 不可绕过。
- [x] 统一输出 Agent、Operation、工具、状态、摘要、证据和错误，可由 Supervisor 直接聚合。
- [x] Supervisor 成功用例已改为实际委派 Device/Log 专业 Agent，跨领域 Operation 和 Local 高风险均有闭环测试。
- [x] 只新增 2 个生产类，没有内部模型循环或重复 Provider/Runtime；全量 292 个后端测试通过。
- [x] 后端打包和隔离 JAR 启动通过，HTTP 返回 200，MCP 工具增加为 62；隔离服务已停止。

## M4-04 Verification Agent

- [x] Registry 注册只读 Verification Agent，Supervisor 只能通过唯一高层工具委派。
- [x] 检查仅允许 Device/Log/App 只读 Operation；写操作在执行前拒绝。
- [x] 结论必须引用已执行检查，输出 `VERIFIED`、`PARTIAL` 或 `INSUFFICIENT`，证据不足不伪装成功。

## M4-05 多模型路由和降级

- [x] 复用现有 Provider Gateway 和已配置 Provider，未增加第二套 Provider 抽象。
- [x] 路由按模型能力、质量上限、成本特征和短期可用性排序，当前模型默认保持首选。
- [x] 只有无正文、无工具副作用的安全阶段允许重试或降级；故障模型进入 30 秒冷却。
- [x] 实际 Provider、模型、重试次数和选型原因进入 Agent Event Trace。

## M4-06 前端职责拆分

- [x] `AiChatPanel` 降到 2000 行以内，纯展示迁入 `AiChatViews`。
- [x] 消息引用和流式分段刷新迁入 `useAiChatStore`，格式化迁入独立纯函数。
- [x] 前端仍不构造 Agent Prompt、不决定确认恢复节点，也不保存第二份权威任务状态。

## M4-07 长回复与 Markdown 稳定性

- [x] AI 正文使用稳定分段追加，流式期间只复制和解析可变尾段。
- [x] 每个 Markdown 段实时解析，屏外段使用 `content-visibility` 延迟布局，不再超过阈值后退化为源码文本。
- [x] 业务正文不截断；持久化时只进行一次完整合并。
- [x] 1M Token 等效压力下生成 668 段，最大段 5997 字符，128MB 堆限制内完整通过。

## M4-08 Agent Trace

- [x] Task API 聚合有序 Agent Event 和脱敏 Tool Audit。
- [x] Trace 包含 Task/Step/Tool/Model/RAG、Token 预算、耗时、重试和错误摘要，不包含 Prompt、密钥和完整工具正文。
- [x] AI 顶部提供当前任务 Trace 入口，切换会话或新任务时不会展示错误任务的 Trace。

## M4-09 至 M4-10 测试与评测

- [x] 复用现有 Fake Provider、Fake Tool、SSE、故障注入、并发、确认恢复、取消、Prompt 注入和重启测试基座。
- [x] 新增 5 个集中式 M4 产品化测试，不创建大量细碎测试文件。
- [x] 内置 Agent/Provider 数据集输出路由准确率、Provider 能力兼容率、重复调用率和基线差异报告。
- [x] 回归报告生成到后端 `target/agent-evaluation/agent-evaluation-report.json`。

## M4-11 本地迁移、备份和恢复

- [x] 保留各 Store 已有 Schema 迁移、校验和、原子写、索引重建和损坏隔离。
- [x] 增加受认证维护状态、流式 ZIP 备份、受控恢复和会话索引恢复接口。
- [x] 恢复校验条目路径、根目录编号和总解压大小，备份文件限制为当前用户权限。
- [x] 单会话损坏不会阻塞其它历史或应用启动。

## M4-12 压力和稳定性

- [x] 1000 会话、1000 任务分页和重启恢复继续由既有 Store 测试覆盖。
- [x] 高频事件并发序号、背压、并发资源锁、进程取消和磁盘配额测试通过。
- [x] 1M Token 等效前端长回复在 128MB 堆限制内通过，无正文截断。
- [x] Electron 主进程和资源准备检查通过，未新增无界线程池或长驻大字符串缓存。
- [x] Electron 实际运行约 1 分钟，Renderer RSS 约 95MB、主进程约 106MB，退出后 Renderer 和 18180 后端均无残留。

## M4-13 灰度和回退

- [x] 多 Agent、模型降级、Trace 和本地维护支持环境变量或 YAML 独立开关。
- [x] 关闭多 Agent 后 Supervisor、专业 Agent 和 Verification Agent 不注册，固定工作流和普通对话保留。
- [x] 隔离启动验证工具数由 63 回退为 57，首页仍返回 HTTP 200。

## M4 总体验收

- [x] 对照 `architecture-assessment.md`，没有引入数据库、消息队列、通用工作流引擎、第二 Provider Runtime 或前端任务状态机。
- [x] 297 个后端测试通过，0 失败、0 错误、0 跳过；前端构建、1M Token 稳定性、Electron 语法和资源准备通过。
- [x] 后端打包和隔离 JAR 启动通过；M4 启用时 MCP 注册 63 个工具，灰度关闭多 Agent 时注册 57 个工具。
